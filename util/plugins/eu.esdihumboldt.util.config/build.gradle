plugins {
  id 'hale.migrated-groovy'
}

hale {
  bundleName = 'Configuration Utilities'
}

dependencies {
  // api dependency as downstream use needs GroovyObject
  api libs.groovy.core
  
  implementation libs.snakeyaml

  testImplementation testLibs.junit4
}
